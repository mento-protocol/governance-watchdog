# Required for the function to be able to look up the Discord Webhook URL in GCP Secret Manager.
# Get it via `gcloud projects list --filter="name:governance-watchdog*" --format="value(projectId)")`
GCP_PROJECT_ID=

# Required for the function to be able to look up the Discord Webhook URL and Telegram Bot Token in GCP Secret Manager.
# Get it via `gcloud secrets list`
DISCORD_WEBHOOK_URL_SECRET_ID=
TELEGRAM_BOT_TOKEN_SECRET_ID=

# Get it via `gcloud secrets versions access latest --secret discord-webhook-url` (or alternatively look it up on the Discord channel's settings => integrations => webhooks)
DISCORD_WEBHOOK_URL=

# Get it via `gcloud secrets versions access latest --secret telegram-bot-token`
TELEGRAM_BOT_TOKEN=

# Get it via inviting @MissRose_bot to the group and then using the `/id` command (please remove the bot after you're done)
TELEGRAM_CHAT_ID=